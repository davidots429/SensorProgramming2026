package com.davidots.planmind.ui.theme

import androidx.compose.ui.graphics.Color

val PrimaryBlue = Color(0xFF3B82F6)      // 맑고 시원한 메인 블루
val PrimaryDarkBlue = Color(0xFF1E3A8A)  // 묵직한 네이비 (강조용)
val SecondaryTeal = Color(0xFF14B8A6)    // 청록색 포인트 (완료 표시)
val BackgroundLight = Color(0xFFF8FAFC)  // 아주 밝은 푸른빛 도는 쿨톤 배경
val SurfaceWhite = Color(0xFFFFFFFF)     // 카드, 캘린더 배경
val TextMain = Color(0xFF0F172A)         // 짙은 슬레이트 (거의 검은색)
val TextSub = Color(0xFF64748B)          // 차분한 회색 (보조 텍스트)